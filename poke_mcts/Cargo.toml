[package]
name = "poke_mcts"
version = "0.1.0"
edition = "2021"

[dependencies]
pkmn-engine = { path = "../pkmn-engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
debug = true
